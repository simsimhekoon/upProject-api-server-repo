<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>자유게시판</title>
    <link rel="stylesheet" href="/css/css.css"
</head>
<body>
    <div class="board_wrap">
        <div class="board_title"> 
            <strong> 자유게시판 </strong>
            <P> 더큰내일센터 7기 인재들의 자유게시판입니다.</P>
        </div>
        <div class="board_list_wrap">
            <div class="board_list">
                <div class="top">
                    <div class="num">번호</div>
                    <div class="title">제목</div>
                    <div class="writer">글쓴이</div>
                    <div class="date">작성일</div>
                    <div class="count">조회</div>
                </div>
                <%
                let data;
                let count = postList.length;
                for(var i = postList.length-1; i>=0; i--)
                {
                    data = postList[i];
                %>
                <div>
                    <div class="num"><%=count%></div>
                    <%count--;%>
                    <div class="title"><a href="/post/post/view?id=<%=data.id%>&num=<%=count+1%>"><%=data.title%></a></div>
                    <div class="writer"><%=data.name%></div>
                    <div class="date"><%=date%></div>
                    <div class="count"><%=data.viewCount%></div>
                </div>
                <%}%>
                </div>

                <div class="board_page">                 
                    <a href="#" class="num" id="first">&lt;&lt;</a>
                    <a href="#" class="num" id="prev">&lt;</a>
                    <a href="#" class="num on">1</a>
                    <%
                    let split = postList.length/5;

                    for(var i = 2; i < split+1; i++)
                    {
                        if(i == 6) break;
                    %>                       
                    <a href="#" class="num" id="page<%=i%>"><%=i%></a>
                    <%}%>                         
                    <a href="#" class="num" id="next">&gt;</a>
                    <a href="#" class="num" id="end">&gt;&gt;</a>               
                </div>

                <form action="/post/post/goToWrite" id="goToWrite" method="get">
                    <div class="bt_wrap">
                        <button type="submit" form="goToWrite" class="on">글쓰기</button>
                    </div>
                </form>
        </div>
    </div>
</body>
<!-- <script src="/js/post.js"></script> -->
</html>